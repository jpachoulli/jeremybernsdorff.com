const menu=document.querySelector('.menu-btn'),nav=document.querySelector('.site-nav');menu?.addEventListener('click',()=>{const o=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(o))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));document.querySelectorAll('[data-carousel]').forEach(shell=>{const track=shell.querySelector('[data-carousel-track]');if(!track)return;const slides=[...track.children],prev=shell.querySelector('[data-prev]'),next=shell.querySelector('[data-next]'),status=shell.querySelector('[data-status]'),dotsWrap=shell.querySelector('[data-dots]');if(!slides.length)return;const dots=slides.map((_,i)=>{const b=document.createElement('button');b.className='carousel-dot';b.type='button';b.setAttribute('aria-label',`Go to item ${i+1}`);b.onclick=()=>track.scrollTo({left:slides[i].offsetLeft-track.offsetLeft,behavior:'smooth'});dotsWrap?.appendChild(b);return b});const idx=()=>{let best=0,d=Infinity;slides.forEach((s,i)=>{const n=Math.abs(s.offsetLeft-track.offsetLeft-track.scrollLeft);if(n<d){d=n;best=i}});return best};const update=()=>{const i=idx();if(status)status.textContent=`${i+1} / ${slides.length}`;dots.forEach((x,n)=>x.classList.toggle('active',n===i));if(prev)prev.disabled=i===0;if(next)next.disabled=i===slides.length-1};const go=n=>{const i=Math.max(0,Math.min(slides.length-1,idx()+n));track.scrollTo({left:slides[i].offsetLeft-track.offsetLeft,behavior:'smooth'})};prev?.addEventListener('click',()=>go(-1));next?.addEventListener('click',()=>go(1));track.addEventListener('scroll',()=>requestAnimationFrame(update),{passive:true});shell.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'){e.preventDefault();go(-1)}if(e.key==='ArrowRight'){e.preventDefault();go(1)}});update()});